# Pipeline Status

*What is in motion right now, across every client. Work at rest lives in
`clients/[client]/library/posts/`; this file only tracks what has not landed yet.*

| Client | Piece | Stage | Waiting on |
|---|---|---|---|
| *(nothing in the pipeline)* | | | |

Stages, in order: `_inbox/` -> `pending-humanizer/` -> `pending-qc/` -> `pending-signoff/` -> published.
