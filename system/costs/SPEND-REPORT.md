# Spend Report

*Generated by `python system/costs/rollup.py` from `generation-ledger.csv` and `subscriptions.csv`.*

**No spend recorded yet.**

Every generation (image, video, audio) is logged in the ledger at the moment it is made. An
unlogged generation is how a bill becomes a surprise.
