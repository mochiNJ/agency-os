# 📁 ASSET ORGANIZATION

> **This file has moved. The single source of truth is now
> [`system/file-system-law.md`](file-system-law.md).**

Everything that used to live here (the "user never files anything" duty, the naming conventions, the
`_inbox/` drop path, the product folder shape) is now a chapter of the law, alongside the parts that were
missing: **when a file dies**, **where a new file goes**, and **the one shape every client uses**.

This stub stays only so older links keep working. Do not add rules here. Add them to the law, where
`python system/structure-check.py` can actually enforce them.

- The law: [`system/file-system-law.md`](file-system-law.md)
- What every folder is for: [`system/folder-registry.md`](folder-registry.md)
- Check the repo obeys it: `python system/structure-check.py`
