# [Client] — Creative Decisions & Test Log
**MANDATORY read for every producing + QC agent on [Client] work: Content Strategist (05), Copywriter (06),
Visual Director (08), Video Producer (09), Quality Controller (16). Read this BEFORE producing/scoring, every
time. This is the running record of what we TESTED, the RESULT, and the FEEDBACK the user/client gave, so
nothing is lost between sessions or agents.**

> Agents are spawned fresh with no memory of their own. This file (plus the canonical rule files it points to)
> IS the memory. The dispatcher keeps it current: after ANY user/client creative feedback or test result, it
> gets logged here AND folded into the canonical rule file, then agents read both. A rule that lives only in
> chat is LOST. (See CLAUDE.md → "FEEDBACK NEVER LIVES ONLY IN CHAT".)

## 📌 The canonical rule files this log sits on top of (read these too)
- `system/prompt-writing-rules.md` — ALL-client image/video prompt rules (Rule 0 identity-first / branded-hero,
  the two-step compose method for FLAT graphics, reference-first, fonts named, Rule 3 rigor, Rule 4 variety,
  Rule 5 serve-the-subject, Rule 6 never alter the product, Rule 7 show prompts + sign off before generating).
- `clients/[client]/visual-remarks.md` — this client's own visual rules (if it exists; overrides on conflict).
- `system/output-quality-rules.md` — quality gate rules (incl. NO em-dashes; videos must have sound).
- the INSPIRATION BOARDS this client inherits (`inspiration-library/recipes/README.md` + the client board).

---

## 🧪 TEST LOG (newest first)

### YYYY-MM-DD — [what we tested]
**What we're testing:** [the piece / format / approach]
**Result:** [what happened, incl. QC score + whether the client accepted it]
**Feedback → rule:** [the exact feedback the user/client gave, and where it now lives as a standing rule]
**Assets:** [paths locked]  ·  **Next:** [the corrected next step]

---

## How to use this file (dispatcher)
After every creative test or user/client feedback: add a dated entry here (what tested / result / feedback),
fold any new standing rule into the canonical file, and tell the user. Do not let a rule live ONLY in chat.
